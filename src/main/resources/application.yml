spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/population?serverTimezone=UTC
    username: root
    password: "!Kqmysql2024"  # Enclose in quotes if the password contains special characters
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect  # Use MySQL5InnoDBDialect
server:
  port: 9191
